# 🚀 วิธี Deploy PurrDrop บน Railway (ฟรี)

## ขั้นตอนที่ 1: สร้าง GitHub Repository

### 1.1 สร้าง repo ใหม่บน GitHub
1. ไปที่ https://github.com/new
2. ตั้งชื่อ repo เช่น `purrdrop`
3. เลือก **Public** (ฟรี)
4. กด **Create repository**

### 1.2 Push โค้ดขึ้น GitHub
เปิด Terminal/Command Prompt ใน folder `critters-web` แล้วรัน:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/purrdrop.git
git push -u origin main
```

**เปลี่ยน `YOUR_USERNAME` เป็น username GitHub ของคุณ**

---

## ขั้นตอนที่ 2: Deploy บน Railway

### 2.1 สมัคร Railway
1. ไปที่ https://railway.app
2. กด **Login** → เลือก **Login with GitHub**
3. อนุญาตให้ Railway เข้าถึง GitHub

### 2.2 สร้าง Project ใหม่
1. กด **New Project**
2. เลือก **Deploy from GitHub repo**
3. เลือก repo `purrdrop` ที่เพิ่ง push ขึ้นไป
4. Railway จะเริ่ม deploy อัตโนมัติ

### 2.3 ตั้งค่า (สำคัญ!)
1. คลิกที่ project ที่สร้าง
2. ไปที่ **Settings** → **Networking**
3. กด **Generate Domain** เพื่อสร้าง URL สาธารณะ
4. รอ deploy เสร็จ (ประมาณ 2-3 นาที)

---

## ขั้นตอนที่ 3: ใช้งาน!

เมื่อ deploy เสร็จ คุณจะได้ URL เช่น:
```
https://purrdrop-production.up.railway.app
```

เปิด URL นี้ในอุปกรณ์ต่างๆ ที่ต้องการส่งไฟล์หากัน!

---

## 💡 Tips

### Railway Free Tier
- ได้ $5 credit ต่อเดือน (ใช้ได้ประมาณ 500 ชั่วโมง)
- ถ้าใช้เยอะ อาจต้องเติมเงิน ($5/เดือน)

### ถ้าอยากใช้ฟรีตลอด
ใช้ **Render.com** แทน:
1. ไปที่ https://render.com
2. สร้าง **Web Service** จาก GitHub repo
3. เลือก **Free** tier
4. ตั้ง Build Command: `npm install && npm run build`
5. ตั้ง Start Command: `npm start`

---

## 🔧 Troubleshooting

### Deploy ไม่ผ่าน?
- เช็คว่า push โค้ดขึ้น GitHub ครบหรือยัง
- ดู logs ใน Railway dashboard

### เชื่อมต่อไม่ได้?
- ตรวจสอบว่าอุปกรณ์ทั้งสองเปิด URL เดียวกัน
- ลอง refresh หน้าเว็บ

### ส่งไฟล์ไม่ได้?
- WebRTC ต้องการ HTTPS (Railway ให้มาแล้ว)
- ถ้าอยู่คนละ network อาจต้องใช้ TURN server

---

## 📱 การใช้งาน

1. เปิด URL ในอุปกรณ์ที่ 1
2. เปิด URL เดียวกันในอุปกรณ์ที่ 2
3. จะเห็น Critter ของอีกเครื่องปรากฏขึ้น
4. คลิกที่ Critter แล้วเลือกไฟล์ที่จะส่ง
5. อีกเครื่องกด "รับเลย!" 
6. ไฟล์จะถูกส่งผ่าน WebRTC โดยตรง!
